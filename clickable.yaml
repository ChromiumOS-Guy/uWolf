clickable_minimum_required: 8.0.0
builder: custom
build: ${ROOT}/scripts/build.sh
dependencies_host: ["qemu-user-static" , "tree" , "patchelf", "git"]
install_lib:
  - /usr/lib/${ARCH_TRIPLET}/libvpx.so*
  - /usr/lib/${ARCH_TRIPLET}/libwebp.so*
ignore_review_warnings: true
skip_review: true
